20210421
1、支持flv
2、增加ArcConverter工具的入口包含当前文件直接转换和所在文件夹文件的批处理
3、增加标注自画控件的支持，arccompareview_setting.txt下ShowDrawPanel=1时，用右键可以在图上画框（直接画框为蓝色，通过坐标转换后为红色）
4、解决高级模式，一直按键盘->,帧同步状态不对的问题
5、解决高级模式，播放到底，切换play状态，再Replay，播放速度异常的问题
6、解决一个潜在的crash问题
7、解决高级模式下，初始化step值偶现不对的问题

20210415
1、支持bmp
2、覆盖式B|A的时候，需要将比较图的view显示状态也保留，而不是单纯fitin显示
3、增加记录上次打开的文件类型的功能（video/image)
4、增加crash dump代码，并增加map文件，用于出现crash时定位问题

20210409
1、解决双屏显示时，从比较显示页面退出到home界面时，偶现界面从一个屏幕跳到另一个屏幕的问题

20210324
1、解决硬解码版本存在轻微卡顿，且在replay的情况下更加严重的问题
2、解决高级模式下快速单帧前进，存在帧同步功能异常的现象  
3、解决内存占用过大；replay或者导入新视频后，内存不会释放，只有在完全关闭后会释放的问题
4、解决第二次打开文件的时候，文件信息按钮状态和实际不符
5、解决两个屏幕的情况下只能在主屏幕打开的问题
6、解决打开文件返回home页面，最小化后，回复窗口后尺寸不对的问题。
7、优化打开文件的性能
8、当文件和exe关联后，程序打开的情况下，双击文件，可以直接跳到比较界面
9、比较界面支持拖动打开文件
10、当打开单个文件时，支持在相同文件夹内切换文件，并支持快捷键Ctrl + <-和Ctrl + ->切换。
11、解决安装好程序后选择启动程序时，打开的界面不对的问题
12、解决多帧数据流播放时，重复replay显示不对的问题
       

20210225
1、解决在一些设备上timer不准确，引起播放卡帧的问题
2、更新GLrender解决RGB565显示不对的问题
3、解决任务栏程序icon不显示的问题
4、解决有的视频普通播放模式replay后进度条不动的问题

20210208
1、如果设备显卡支持ffmpeg CUDA优化（NVIDIA），则采用硬解，否则软解。

20210207
1、解决版本版本信息中带项目号的问题

20210204
1、home页面增加help按钮，可以弹出使用说明对话框
2、多帧YUV数据流增加支持文件名带video的yuv格式（video_wxh.nv21）
3、多帧YUV数据播放，支持上下箭头快捷键调节播放的FPS（默认30fps） 
4、比较窗口增加标题栏，支持窗口最大化，最小化，双击标题栏全屏，按ESC退出全屏 
5、支持文件选中右键打开方式。
6、JPG转YUV时直接转成默认的编码，而不是目标YUV format
7、增加图片exif信息和Video的编码信息，显示文件名tip。
8、Fix bug：
   1）解决多帧YUV数据暂停，拖动到最后，然后往后切图时出现绿帧问题
   2）解决多帧YUV数据播放，步长改为1，后退一帧效果不对的问题
   3）解决RGB图片颜色显示不对的问题。
   4）解决Video播放时，水印添加不生效的问题
   5）解决每次拖动打开，文件排列顺序不一致的问题


20210119
1、增加H265 10bit编码视频的的播放
2、解决播放时间不能到底的问题
3、解决文件名显示有锯齿的问题
4、解决安装包直接运行程序后，不能拖动打开的问题
5、高级播放模式的步长编辑，除了回车确认外，支持前进和后退按钮确认修改
6、增加BT601_OPENCV_FULL的colorspace，解决有时候colorspace设置不生效的问题


20210108
1、增加P010的图片显示支持
2、增加多帧P010数据流显示支持
3、拖动打开不支持的格式时增加界面提示

20201225
1、更新主界面UI，增加拖动打开功能
2、更新比较界面UI，增加右转90度的功能
3、增加水印，可以在setting.txt中通过ShowWatermark值进行关闭，默认为1打开。

20201218
1、解决普通播放模式下快速拖动UI引起的crash问题
2、工具条增加自动适合和文件信息的checkbox，支持相关功能。
3、解决普通播放模式不能按照fps播放的问题。

20201204
1、增加多帧YUV视频流的播放支持，文件名为str_wxh.mnv21的形式
2、增加QT插件中供标记标板使用的坐标转换接口，setting中ShowDrawPanel=1时，会显示标记画板，支持鼠标右键画一个矩形框（蓝色），缩放或者移动图后根据接口转换出红色矩形框。默认关闭

20201125
1、视频播放分为普通播放（原来的自由播放）和高级播放模式（严格同步播放，支持按帧前进后退）
2、两种播放模式都支持按下ctrl按键进行UI操作单独控制对应窗口，否则为所有窗口同时控制，支持空格快捷键统一暂停/播放
3、当两个视频文件比较时，支持覆盖式before/after,只在暂停状态有效
4、高级播放模式的帧前进后退功能只有在播放暂停状态下有效，支持快捷键<-和->统一前进后退
5、支持配置显示的RGB颜色空间：JPG，BT601，BT709和BT2020，为setting.txt下的RGBColorSpace值。
6、高级播放模式支持全屏模式，当只有两个文件时，只会左右各半的比较方式。

20201105
1、简单单独自由播放，不做视频同步，
2、增加ctrl按键，按下ctrl按键进行UI操作时，为比较窗口单独控制，否则为比较窗口同时控制
3、增加空格快捷键支持所有比较窗口统一控制暂停/播放
4、去掉过期时间

20200911
1、简单单独自由播放，不做视频同步，video播放UI操作为比较窗口单独控制
2、图像UI操作1:1，fitin，放大（ctrl+），缩小（ctrl-）为比较窗口绑定同步控制，旋转按钮为比较窗口单独控制
3、图像放大后支持拖动，为比较窗口绑定同步拖动
4、增加空格快捷键支持所有比较窗口统一控制暂停/播放